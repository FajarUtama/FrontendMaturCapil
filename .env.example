# Salin ke .env lokal: cp .env.example .env

# true (default) = mock + localStorage | false = backend nyata (setelah ApiDataProvider siap)
VITE_USE_MOCK_API=true

# Base URL API (tanpa trailing slash)
VITE_API_BASE_URL=http://localhost:3000/api/v1

# Timeout request (ms)
VITE_API_TIMEOUT_MS=30000
